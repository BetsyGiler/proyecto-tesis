/**
 * Enum to define all the available routes on the system
 */
export enum AuthRoutes {
	LOGIN = '/auth/login',
	REGISTER = '/auth/register',
	CHECK_SESSION = '/auth/check-session',
	LOGOUT = '/auth/logout',
};

export enum Routes {
	// User routes
	USER = '/user',
};
